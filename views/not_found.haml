- @title = capture_haml do
  - if context == :user
    I don’t know this person.
  - else
    Sorry, what?

.ill-tell-you-what
  - if context == :user
    %p
      This isn’t a username I recognize. Is it you? Let’s get acquainted!

    %p.cta
      %a{ href: '/auth/spotify' }
        Sign in with&nbsp;Spotify

  - else
    %p
      This is probably my bad, but I just have no idea where you were trying to
      go.

    %p
      If this seems like a goof, would you mind letting me know? I’m
      <a href="http://twitter.com/johnholdun">@johnholdun</a>,
      in case you missed that before.
